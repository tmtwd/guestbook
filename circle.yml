machine:
  java:
    version: openjdk8
    # post:
    # - lein run:
    #       background: true
    # - sleep 5
dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get -y install xvfb
    - sudo apt-get install firefox
