machine:
  java:
    version: openjdk8
  # post:
  #   - lein run:
  #     background: true
dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get update && sudo apt-get install libpango1.0-0 && sudo apt-get install firefox

# test:
#   override:
#     - xvfb-run -a lein test
